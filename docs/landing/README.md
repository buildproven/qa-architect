# Landing page source

`index.html` is the source for the marketing/checkout page at `buildproven.ai/qa-architect`. Self-contained (no build step, no external CDN calls) — copy the file as-is to wherever it's hosted.

The Pro call to action points to the canonical product page at
`https://buildproven.ai/qa-architect`. Keep checkout configuration on that
hosted product page; do not embed provider-specific product IDs in this source.
