# Landing page source

`index.html` is the source for the marketing/checkout page at `buildproven.ai/qa-architect`. Self-contained (no build step, no external CDN calls) — copy the file as-is to wherever it's hosted.

Before publishing:

1. Replace the Pro checkout `href` (search for `REPLACE_CHECKOUT_URL`) with the live Polar.sh checkout link.
2. Confirm the GitHub link (`github.com/buildproven-ai/qa-architect`) matches the actual public repo URL.
